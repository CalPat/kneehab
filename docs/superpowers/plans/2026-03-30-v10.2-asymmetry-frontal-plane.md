# KneeHab v10.2 — Asymmetry Rule + Frontal Plane Rebuild Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Update `index.html` to v10.2 — add Asymmetry Rule protocol, trim Load day blocks, replace Skater Squat with Curtsy + Cossack, move Calf Raises to Optional.

**Architecture:** All changes are content edits to a single-file app (`index.html`). Two data layers need to stay in sync: (1) the `BLOCKS` JS object (~line 2711) which drives the Today tab, and (2) `src-*` HTML elements (~line 1992+) which drive the Reference hub. Every exercise change touches both layers.

**Tech Stack:** Vanilla HTML/CSS/JS single-file app. No build step. No test suite — verification is grep confirmation + manual browser check at end.

---

## File Map

| File | Action | Purpose |
|---|---|---|
| `index.html` | Modify | All changes — BLOCKS JS + src-* HTML elements |
| `kneehab V10.2.html` | Create | Versioned snapshot (copy of final index.html) |
| `kneehab V10.2.MD` | Create | Summary doc per CLAUDE.md protocol |

---

## Task 1: Version strings

**Files:**
- Modify: `index.html` (lines 9, 1163, 1270, 2446)

- [ ] **Step 1: Update `<title>` tag**

Find:
```
<title>Knee Rehab v10.1</title>
```
Replace with:
```
<title>Knee Rehab v10.2</title>
```

- [ ] **Step 2: Update menu title**

Find:
```
<div class="ms-title">KNEE REHAB v10.1</div>
```
Replace with:
```
<div class="ms-title">KNEE REHAB v10.2</div>
```

- [ ] **Step 3: Update cover heading**

Find:
```
<div class="cover-h1">KNEE<br>REHAB<br><em>v10.1</em></div>
```
Replace with:
```
<div class="cover-h1">KNEE<br>REHAB<br><em>v10.2</em></div>
```

- [ ] **Step 4: Update footer version**

Find:
```
Not a substitute for clinical diagnosis or physiotherapy. · v10.1 — March 2026
```
Replace with:
```
Not a substitute for clinical diagnosis or physiotherapy. · v10.2 — March 2026
```

- [ ] **Step 5: Verify**

Run: `grep -c "v10.2" index.html`
Expected: at least 4 matches. Run `grep -c "v10.1" index.html` — should be 0 outside the changelog section (changelog entries referencing v10.1 are fine to keep).

- [ ] **Step 6: Commit**
```bash
git add index.html
git commit -m "v10.2 — bump version strings"
```

---

## Task 2: Load Day briefing — Asymmetry Rule

**Files:**
- Modify: `index.html` (~line 2715, BLOCKS 'A' note field)

- [ ] **Step 1: Add Asymmetry Rule to Load Day briefing**

Find:
```
note:'Loading day. Never run two in a row — always follow with Recovery or Activity. Bike is optional.',
```
Replace with:
```
note:'Loading day. Never run two in a row — always follow with Recovery or Activity. Bike is optional. <strong>Asymmetry Rule:</strong> On all alternating exercises, start with the weaker side and add one extra set to the weaker side.',
```

- [ ] **Step 2: Verify**

Run: `grep -c "Asymmetry Rule" index.html`
Expected: at least 1 match.

- [ ] **Step 3: Commit**
```bash
git add index.html
git commit -m "v10.2 — add Asymmetry Rule to Load Day briefing"
```

---

## Task 3: Mobility block — reduce Couch Stretch

**Files:**
- Modify: `index.html` (~line 2725, sA_mob detail in BLOCKS JS)

- [ ] **Step 1: Reduce Couch Stretch to 1 set in BLOCKS JS**

Find (in sA_mob detail):
```
{n:'Couch Stretch',rx:'2 × 30s each side',d:'Targets rectus femoris — the hip flexor that crosses the knee joint and directly increases patellofemoral pressure when tight.',timer:30}
```
Replace with:
```
{n:'Couch Stretch',rx:'1 × 30s each side',d:'Targets rectus femoris — the hip flexor that crosses the knee joint and directly increases patellofemoral pressure when tight. Full volume on Recovery day.',timer:30}
```

- [ ] **Step 2: Verify**

Run: `grep "Couch Stretch" index.html`
The sA_mob entry should show `1 × 30s`. The sC_mob entry (line ~2783) should still show `2 × 30s` — do not change that one.

- [ ] **Step 3: Commit**
```bash
git add index.html
git commit -m "v10.2 — reduce Couch Stretch to 1×30s on Load day"
```

---

## Task 4: Spinal & Shoulder — remove Face Pull from Load Day

**Files:**
- Modify: `index.html` (~line 2729, sA_gap detail in BLOCKS JS)

Note: Only remove from BLOCKS 'A' (Today tab). Face Pull stays on Recovery day (sC_gap) and in the base HTML reference layer — do not touch those.

- [ ] **Step 1: Remove Face Pull from sA_gap detail**

Find (the full sA_gap block):
```
      {id:'sA_gap',  time:'T+12',   title:'Spinal &amp; Shoulder',    sub:'~8 min · filling collagen window', navPage:'base',
       detail:[{n:'Bird Dog',rx:'90s total',d:'Anti-rotation posterior chain activation. Builds the co-contraction stability needed for single-leg loaded movements.',timer:90},
               {n:'WLYT',rx:'60s total',d:'Lower and mid trapezius activation. Corrects the scapular depression deficit created by sustained desk posture.',timer:60},
               {n:'Face Pull',rx:'2 × 15',d:'Scapular retraction and shoulder external rotation combined. Corrects internal rotation dominance from desk work.'}]},
```
Replace with:
```
      {id:'sA_gap',  time:'T+12',   title:'Spinal &amp; Shoulder',    sub:'~6 min · filling collagen window', navPage:'base',
       detail:[{n:'Bird Dog',rx:'90s total',d:'Anti-rotation posterior chain activation. Builds the co-contraction stability needed for single-leg loaded movements.',timer:90},
               {n:'WLYT',rx:'60s total',d:'Lower and mid trapezius activation. Corrects the scapular depression deficit created by sustained desk posture.',timer:60}]},
```

- [ ] **Step 2: Verify**

Run: `grep -A5 "sA_gap" index.html`
Expected: sA_gap detail contains Bird Dog and WLYT only. Face Pull should not appear in sA_gap. Face Pull should still appear in sC_gap.

- [ ] **Step 3: Commit**
```bash
git add index.html
git commit -m "v10.2 — remove Face Pull from Load day (stays on Recovery)"
```

---

## Task 5: Core block — remove RKC Plank from Load Day

**Files:**
- Modify: `index.html` (~line 2730, sA_core detail in BLOCKS JS)

Note: Only remove from BLOCKS 'A'. RKC Plank stays on Recovery day (sC_core) — do not touch that.

- [ ] **Step 1: Remove RKC Plank from sA_core detail**

Find (the full sA_core block):
```
      {id:'sA_core', time:'T+20',   title:'Core',                       sub:'~4 min', navPage:'base',
       detail:[{n:'Hollow Body Hold',rx:'60s total',d:'Anterior core tension under spinal neutral. Trains the bracing position that supports loaded single-leg work.',timer:60},
               {n:'Side Plank',rx:'90s total · both sides',d:'Lateral core and hip stability. Prevents trunk lateral shift during single-leg exercises.',timer:90},
               {n:'RKC Plank',rx:'2 × 30s',d:'Full-body co-contraction plank. Significantly harder than a standard plank — trains simultaneous global tension.',timer:30}]},
```
Replace with:
```
      {id:'sA_core', time:'T+20',   title:'Core',                       sub:'~3 min', navPage:'base',
       detail:[{n:'Hollow Body Hold',rx:'60s total',d:'Anterior core tension under spinal neutral. Trains the bracing position that supports loaded single-leg work.',timer:60},
               {n:'Side Plank',rx:'90s total · both sides',d:'Lateral core and hip stability. Prevents trunk lateral shift during single-leg exercises — directly supports Curtsy Squat and Cossack Squat form.',timer:90}]},
```

- [ ] **Step 2: Verify**

Run: `grep -A5 "sA_core" index.html`
Expected: sA_core has Hollow Body Hold and Side Plank only. RKC Plank should still appear in sC_core.

- [ ] **Step 3: Commit**
```bash
git add index.html
git commit -m "v10.2 — remove RKC Plank from Load day core (stays on Recovery)"
```

---

## Task 6: Glute block — remove Supine Hip ER, update Hip Abduction + Adductor ISO

**Files:**
- Modify: `index.html` (~line 2734, sA_glut in BLOCKS JS; ~line 1992, src-glute HTML)

- [ ] **Step 1: Update sA_glut in BLOCKS JS**

Find (the full sA_glut block):
```
      {id:'sA_glut', time:'T+24',   title:'Glute',                      sub:'~3 min', navPage:'load',
       detail:[{n:'Adductor Side-Lying Isometric Hold',rx:'3 × 30s each side',d:'Adductor tissue loading — the required bridge before Copenhagen Plank. Builds adductor longus and magnus tolerance the CPH moment arm demands. After 2–4 weeks pain-free, progress to Copenhagen Plank and replace this exercise.'},
               {n:'Supine Hip External Rotation with Block',rx:'3 × 30–45s each side',d:'Deep hip rotator isometric. Targets piriformis and obturator group — rotational control that hip abduction does not address.'},
               {n:'Hip Abduction',rx:'2 × 12 each · 2s hold top',d:'Glute medius isolation in the frontal plane. Controls contralateral pelvic drop and femoral adduction/IR under single-leg load — the primary upstream driver of dynamic knee valgus. Side-lying, pelvis stacked. Keep pelvis neutral — do not rotate backward on the raise. Band at ankle optional to increase demand.'},
               {n:'Single-Leg Glute Bridge',rx:'2 × 10 each · 2s hold top',d:'Posterior chain integration under single-leg load. Real glute load — excluded from Recovery day for this reason.'}]},
```
Replace with:
```
      {id:'sA_glut', time:'T+24',   title:'Glute',                      sub:'~3 min', navPage:'load',
       detail:[{n:'Adductor Side-Lying Isometric Hold',rx:'3 × 30s each side',d:'Adductor tissue loading — the required bridge before Copenhagen Plank. Builds adductor longus and magnus tolerance the CPH moment arm demands. After 2–4 weeks pain-free, progress to Copenhagen Plank and replace this exercise. Apply asymmetry rule — weaker side first, extra set if one side lags.'},
               {n:'Hip Abduction',rx:'3 × 12 weak / 2 × 12 strong · 2s hold top',d:'Glute medius isolation in the frontal plane. Controls contralateral pelvic drop and femoral adduction/IR under single-leg load — the primary upstream driver of dynamic knee valgus. Side-lying, pelvis stacked. Keep pelvis neutral — do not rotate backward on the raise. Band at ankle optional. Apply asymmetry rule — weaker side first, extra set weaker side.'},
               {n:'Single-Leg Glute Bridge',rx:'2 × 10 each · 2s hold top',d:'Posterior chain integration under single-leg load. Real glute load — excluded from Recovery day for this reason.'}]},
```

- [ ] **Step 2: Update src-glute HTML (Reference hub)**

Find (lines 1993–1995, the three ex-card divs inside src-glute):
```
      <div class="ex-card"><div class="ex-top"><span class="ex-name">Adductor Side-Lying Isometric Hold</span><span class="ex-rx">3 × 30s each side</span></div><div class="ex-desc">Side-lying. Feet stacked or slight offset. Squeeze adductors isometrically — hold 30s each side. Targets adductor longus and magnus. Builds the tissue tolerance required before Copenhagen Plank can safely load the groin. <strong>Next lever:</strong> after 2–4 weeks pain-free, progress to Copenhagen Plank — replace this exercise. Do not run both simultaneously.</div></div>
      <div class="ex-card"><div class="ex-top"><span class="ex-name">Supine Hip External Rotation with Block</span><span class="ex-rx">3 × 30–45s each side</span></div><div class="ex-desc">On back, knees bent, feet flat. Block between knees — anchors opposite hip so it cannot compensate. Let one knee drop outward to comfortable end-range external rotation; hold 30–45s. Targets deep rotators (piriformis, obturator group, gemelli) via end-range isometric — not loaded by hip abduction. Zero knee compression. Progress: light band around thigh pulling into IR to resist against.</div></div>
      <div class="ex-card"><div class="ex-top"><span class="ex-name">Hip Abduction</span><span class="ex-rx">2 × 12 each · 2s hold top</span></div><div class="ex-desc">Side-lying, bottom knee bent for stability. Top leg straight, raise to ~30–45°, 2s hold at top, lower controlled. Keep pelvis stacked — do not rotate backward on the raise. Glute medius isolation in the frontal plane. Prevents contralateral pelvic drop and femoral adduction/IR under single-leg load — the primary upstream driver of dynamic knee valgus. Band at ankle optional for added resistance.</div></div>
```
Replace with:
```
      <div class="ex-card"><div class="ex-top"><span class="ex-name">Adductor Side-Lying Isometric Hold</span><span class="ex-rx">3 × 30s each side</span></div><div class="ex-desc">Side-lying. Feet stacked or slight offset. Squeeze adductors isometrically — hold 30s each side. Targets adductor longus and magnus. Builds the tissue tolerance required before Copenhagen Plank can safely load the groin. <strong>Next lever:</strong> after 2–4 weeks pain-free, progress to Copenhagen Plank — replace this exercise. Do not run both simultaneously. Apply asymmetry rule — weaker side first, extra set if one side lags.</div></div>
      <div class="ex-card"><div class="ex-top"><span class="ex-name">Supine Hip External Rotation with Block</span><span class="ex-rx">3 × 30–45s each side</span></div><div class="ex-desc">On back, knees bent, feet flat. Block between knees — anchors opposite hip so it cannot compensate. Let one knee drop outward to comfortable end-range external rotation; hold 30–45s. Targets deep rotators (piriformis, obturator group, gemelli) via end-range isometric — not loaded by hip abduction. Zero knee compression. Progress: light band around thigh pulling into IR to resist against. <em>Recovery day only — removed from Load day in v10.2.</em></div></div>
      <div class="ex-card"><div class="ex-top"><span class="ex-name">Hip Abduction</span><span class="ex-rx">3 × 12 weak / 2 × 12 strong · 2s hold top</span></div><div class="ex-desc">Side-lying, bottom knee bent for stability. Top leg straight, raise to ~30–45°, 2s hold at top, lower controlled. Keep pelvis stacked — do not rotate backward on the raise. Glute medius isolation in the frontal plane. Prevents contralateral pelvic drop and femoral adduction/IR under single-leg load — the primary upstream driver of dynamic knee valgus. Band at ankle optional for added resistance. Apply asymmetry rule — weaker side first, extra set weaker side.</div></div>
```

- [ ] **Step 3: Verify**

Run: `grep "Hip Abduction" index.html`
Expected: both instances show `3 × 12 weak / 2 × 12 strong`. `Supine Hip External Rotation` should still appear (in src-glute and sC_glut) but not in sA_glut.

- [ ] **Step 4: Commit**
```bash
git add index.html
git commit -m "v10.2 — glute block: remove Supine Hip ER from Load day, add asymmetry cues to Hip Ab + Adductor ISO"
```

---

## Task 7: Knee block — update Step Down, remove Skater Squat

**Files:**
- Modify: `index.html` (~line 2741–2744, sA_knee detail in BLOCKS JS; ~line 2048–2051, src-knee HTML)

- [ ] **Step 1: Update Step Down in BLOCKS JS (add pre-activation cue)**

Find:
```
               {n:'Slant Board Step Down',rx:'2×10 primary · 2×8 secondary',d:'Eccentric quad load under single-leg knee control. The 2s pause mimics the sustained loaded position of ski descent.'},
```
Replace with:
```
               {n:'Slant Board Step Down',rx:'2×10 primary · 2×8 secondary',d:'Eccentric quad load under single-leg knee control. The 2s pause mimics the sustained loaded position of ski descent. <strong>Weaker side pre-activation:</strong> before each set on the weaker side, hold single-leg stance for 5s — focus on level pelvis, glute med actively holding — then begin the descent.'},
```

- [ ] **Step 2: Remove Skater Squat from BLOCKS JS**

Find:
```
               {n:'Skater Squat',rx:'2 × 8 each · controlled',d:'Single-leg eccentric quad with high proprioceptive demand. Genuinely complementary to the step-down — not redundant. Always start assisted.'},
```
Replace with: *(delete the line — replace with empty string)*

- [ ] **Step 3: Update Step Down in src-knee HTML**

Find:
```
      <div class="ex-card"><div class="ex-top"><span class="ex-name">Slant Board Step Down</span><span class="ex-rx">2×10 primary · 2×8 secondary</span></div><div class="ex-desc">10cm step Phase 1. Lower opposite foot over 5s, 2s pause at bottom (foot hovering, not touching), return. Hand on wall → unassisted. The 2s pause converts eccentric to eccentric-plus-isometric — directly mimics sustained ski loaded position. <strong>Progression levers (2×2 rule):</strong> 10cm→15cm→20cm→25cm / 3s→4s eccentric / bodyweight→KB at chest.</div></div>
```
Replace with:
```
      <div class="ex-card"><div class="ex-top"><span class="ex-name">Slant Board Step Down</span><span class="ex-rx">2×10 primary · 2×8 secondary</span></div><div class="ex-desc">10cm step Phase 1. Lower opposite foot over 5s, 2s pause at bottom (foot hovering, not touching), return. Hand on wall → unassisted. The 2s pause converts eccentric to eccentric-plus-isometric — directly mimics sustained ski loaded position. <strong>Weaker side pre-activation:</strong> before each set on the weaker side, hold single-leg stance for 5s — level pelvis, glute med actively holding — then begin the descent. <strong>Progression levers (2×2 rule):</strong> 10cm→15cm→20cm→25cm / 3s→4s eccentric / bodyweight→KB at chest.</div></div>
```

- [ ] **Step 4: Remove Skater Squat from src-knee HTML**

Find:
```
      <div class="ex-card"><div class="ex-top"><span class="ex-name">Skater Squat</span><span class="ex-rx">2 × 8 each · controlled</span></div><div class="ex-desc">Single-leg squat, trailing leg sweeps laterally. Higher proprioceptive demand than step-up — genuinely complementary, not redundant. <strong>Start:</strong> one hand lightly touching doorframe or TRX, partial depth. Do not start unassisted — compensation patterns are hard to undo. <strong>Levers (2×2 rule):</strong> two-finger touch→fingertip→unassisted / partial→full depth / standard→3s eccentric / bodyweight→KB goblet / stable→slant board Phase 3.</div></div>
```
Replace with: *(delete the line — replace with empty string)*

- [ ] **Step 5: Verify**

Run: `grep "Skater Squat" index.html`
Expected: matches only in the changelog section (historical entries), not in sA_knee detail or src-knee. Run `grep "pre-activation" index.html` — expected: 1 match in BLOCKS JS, 1 match in src-knee.

- [ ] **Step 6: Commit**
```bash
git add index.html
git commit -m "v10.2 — knee block: add Step Down pre-activation cue, remove Skater Squat"
```

---

## Task 8: Add Curtsy Squat

**Files:**
- Modify: `index.html` (sA_knee BLOCKS JS after Split Squat; src-knee HTML after Split Squat card)

- [ ] **Step 1: Add Curtsy Squat to sA_knee BLOCKS JS**

Find (Split Squat entry in sA_knee):
```
               {n:'Split Squat Progression',rx:'2 × 8–10 each side · bodyweight Ph1',d:'Rear leg extended throughout — not bent to 90°. Extended rear leg stretches rectus femoris under load, directly addressing a primary PFPS driver.'},
```
Replace with:
```
               {n:'Split Squat Progression',rx:'2 × 8–10 each side · bodyweight Ph1',d:'Rear leg extended throughout — not bent to 90°. Extended rear leg stretches rectus femoris under load, directly addressing a primary PFPS driver.'},
               {n:'Curtsy Squat',rx:'3 × 8 weak / 2 × 8 strong · 2s descent',d:'Rear foot crosses behind and outside stance foot. Stance-leg glute med resists femoral adduction throughout — trains the exact deficit pattern. Keep stance-leg knee tracking over 2nd–3rd toe; do not let it cave medially. Trunk upright. Apply asymmetry rule — weaker side first. <strong>Regression:</strong> reduce depth, hold door frame. <strong>Progression:</strong> 3–4s eccentric → light goblet weight.'},
```

- [ ] **Step 2: Add Curtsy Squat to src-knee HTML**

Find (Split Squat card in src-knee):
```
      <div class="ex-card"><div class="ex-top"><span class="ex-name">Split Squat Progression</span><span class="ex-rx">2 × 8–10 each side · bodyweight Ph1</span></div><div class="ex-desc">Rear leg extended throughout — not bent to 90°. Extended rear leg stretches rectus femoris under load, directly addressing a primary PFPS driver. <strong>Phase 1 constraints:</strong> step length moderate, trunk upright, knee at or inside toes, bodyweight only. <strong>Levers (2×2 rule):</strong> partial→full depth / standard→3s eccentric / flat→rear foot elevated / bodyweight→KB goblet→KB each hand / stable→slant board Phase 3.</div></div>
```
Replace with:
```
      <div class="ex-card"><div class="ex-top"><span class="ex-name">Split Squat Progression</span><span class="ex-rx">2 × 8–10 each side · bodyweight Ph1</span></div><div class="ex-desc">Rear leg extended throughout — not bent to 90°. Extended rear leg stretches rectus femoris under load, directly addressing a primary PFPS driver. <strong>Phase 1 constraints:</strong> step length moderate, trunk upright, knee at or inside toes, bodyweight only. <strong>Levers (2×2 rule):</strong> partial→full depth / standard→3s eccentric / flat→rear foot elevated / bodyweight→KB goblet→KB each hand / stable→slant board Phase 3.</div></div>
      <div class="ex-card"><div class="ex-top"><span class="ex-name">Curtsy Squat</span><span class="ex-rx">3 × 8 weak / 2 × 8 strong · 2s descent</span></div><div class="ex-desc">Rear foot crosses behind and outside stance foot — crossed stance. Stance-leg glute med resists femoral adduction throughout; this is the primary training stimulus. Keep stance-leg knee tracking over 2nd–3rd toe — do not let it cave medially. Trunk upright throughout. Apply asymmetry rule — weaker side first. <strong>Regression:</strong> hold door frame or TRX for balance, reduce depth. <strong>Progression:</strong> 3–4s eccentric descent → light goblet weight. <strong>Watch for:</strong> stance-knee medial cave (glute med failing), trunk lateral lean (same pattern as step down — stop and reset).</div></div>
```

- [ ] **Step 3: Verify**

Run: `grep -c "Curtsy Squat" index.html`
Expected: 2 matches (one in BLOCKS JS, one in src-knee).

- [ ] **Step 4: Commit**
```bash
git add index.html
git commit -m "v10.2 — add Curtsy Squat to knee block (replaces Skater Squat)"
```

---

## Task 9: Add Cossack Squat

**Files:**
- Modify: `index.html` (sA_knee BLOCKS JS after Curtsy Squat; src-knee HTML after Curtsy Squat card)

- [ ] **Step 1: Add Cossack Squat to sA_knee BLOCKS JS**

Find (the Curtsy Squat entry just added):
```
               {n:'Curtsy Squat',rx:'3 × 8 weak / 2 × 8 strong · 2s descent',d:'Rear foot crosses behind and outside stance foot. Stance-leg glute med resists femoral adduction throughout — trains the exact deficit pattern. Keep stance-leg knee tracking over 2nd–3rd toe; do not let it cave medially. Trunk upright. Apply asymmetry rule — weaker side first. <strong>Regression:</strong> reduce depth, hold door frame. <strong>Progression:</strong> 3–4s eccentric → light goblet weight.'},
```
Replace with:
```
               {n:'Curtsy Squat',rx:'3 × 8 weak / 2 × 8 strong · 2s descent',d:'Rear foot crosses behind and outside stance foot. Stance-leg glute med resists femoral adduction throughout — trains the exact deficit pattern. Keep stance-leg knee tracking over 2nd–3rd toe; do not let it cave medially. Trunk upright. Apply asymmetry rule — weaker side first. <strong>Regression:</strong> reduce depth, hold door frame. <strong>Progression:</strong> 3–4s eccentric → light goblet weight.'},
               {n:'Cossack Squat',rx:'3 × 5 weak / 2 × 5 strong · 3s descent · 3s ascent',d:'Feet wider than shoulder-width. Shift weight to one side, bending that knee while the other leg straightens. Toes on bent leg turned out ~15–20°. Stop at first sign of trunk lateral lean or knee cave — that is your working depth. Start assisted (door frame or TRX) until pattern is stable. Apply asymmetry rule — weaker side first. <strong>Regression ladder:</strong> (1) Assisted — door frame or TRX; (2) Elevated heel — heel on plate, removes dorsiflexion restriction; (3) Partial depth — full bodyweight, limited ROM; (4) Full depth; (5) Weighted — goblet hold. Start at R1 regardless of apparent ability.'},
```

- [ ] **Step 2: Add Cossack Squat to src-knee HTML**

Find (the Curtsy Squat card just added):
```
      <div class="ex-card"><div class="ex-top"><span class="ex-name">Curtsy Squat</span><span class="ex-rx">3 × 8 weak / 2 × 8 strong · 2s descent</span></div><div class="ex-desc">Rear foot crosses behind and outside stance foot — crossed stance. Stance-leg glute med resists femoral adduction throughout; this is the primary training stimulus. Keep stance-leg knee tracking over 2nd–3rd toe — do not let it cave medially. Trunk upright throughout. Apply asymmetry rule — weaker side first. <strong>Regression:</strong> hold door frame or TRX for balance, reduce depth. <strong>Progression:</strong> 3–4s eccentric descent → light goblet weight. <strong>Watch for:</strong> stance-knee medial cave (glute med failing), trunk lateral lean (same pattern as step down — stop and reset).</div></div>
```
Replace with:
```
      <div class="ex-card"><div class="ex-top"><span class="ex-name">Curtsy Squat</span><span class="ex-rx">3 × 8 weak / 2 × 8 strong · 2s descent</span></div><div class="ex-desc">Rear foot crosses behind and outside stance foot — crossed stance. Stance-leg glute med resists femoral adduction throughout; this is the primary training stimulus. Keep stance-leg knee tracking over 2nd–3rd toe — do not let it cave medially. Trunk upright throughout. Apply asymmetry rule — weaker side first. <strong>Regression:</strong> hold door frame or TRX for balance, reduce depth. <strong>Progression:</strong> 3–4s eccentric descent → light goblet weight. <strong>Watch for:</strong> stance-knee medial cave (glute med failing), trunk lateral lean (same pattern as step down — stop and reset).</div></div>
      <div class="ex-card"><div class="ex-top"><span class="ex-name">Cossack Squat</span><span class="ex-rx">3 × 5 weak / 2 × 5 strong · 3s descent · 3s ascent</span></div><div class="ex-desc">Feet wider than shoulder-width. Shift weight to one side, bending that knee while the other leg straightens. Toes on bent leg turned out ~15–20°. Chest tall — do not fold forward. Stop depth at first sign of trunk lateral lean or knee cave — that is your working depth. Apply asymmetry rule — weaker side first.<br><br><strong>Regression ladder (start at R1):</strong><br>R1 — Assisted: hold door frame or TRX, light grip for balance only.<br>R2 — Elevated heel: rear heel on a plate — removes dorsiflexion restriction.<br>R3 — Partial depth: full bodyweight, limited ROM, no assistance.<br>R4 — Full depth: target.<br>R5 — Weighted: goblet hold, loaded progression.<br><br><strong>Watch for:</strong> trunk lateral lean toward bent leg (glute med failing), bent-leg knee cave (valgus), straight-leg hip hike instead of true adductor length.</div></div>
```

- [ ] **Step 3: Verify**

Run: `grep -c "Cossack Squat" index.html`
Expected: 2 matches in the main program (BLOCKS JS + src-knee), plus historical changelog references are acceptable.

- [ ] **Step 4: Commit**
```bash
git add index.html
git commit -m "v10.2 — add Cossack Squat to knee block (mandatory, with regression ladder)"
```

---

## Task 10: Move Calf Raises to Optional pool + update timing

**Files:**
- Modify: `index.html` (sA_knee BLOCKS JS; sA_opt BLOCKS JS; src-knee HTML; src-opt HTML)

- [ ] **Step 1: Remove Calf Raises from sA_knee BLOCKS JS + update timing**

Find (in sA_knee detail, the two calf raise entries):
```
               {n:'Single-Leg Calf Raise — Straight Knee',rx:'1 × 15 each · 3s down · Ph1',d:'Gastrocnemius load. Reduced to 1 set Phase 1 — soleus is higher priority. Full volume returns Phase 2+.'},
               {n:'Single-Leg Calf Raise — Bent Knee (Soleus)',rx:'2 × 12–15 each · 3s down',d:'Soleus isolation — primary plantarflexor under all knee-flexed positions: wall sit, ski descent, split squat. Higher priority of the two calf raises.'},
```
Replace with: *(delete both lines — replace with empty string)*

Also update sA_knee sub timing. Find:
```
      {id:'sA_knee', time:'T+28',   title:'Knee & Lower Body',          sub:'~17 min · collagen rising into peak', navPage:'load',
```
Replace with:
```
      {id:'sA_knee', time:'T+28',   title:'Knee & Lower Body',          sub:'~20 min · collagen rising into peak', navPage:'load',
```

- [ ] **Step 2: Add Calf Raises to sA_opt BLOCKS JS**

Find (the last entry in sA_opt detail, Copenhagen Plank):
```
               {n:'Copenhagen Plank — Short Lever',rx:'3 × 20–30s each side',d:'Phase 2+ adductor progression — replaces Adductor Side-Lying ISO after 2–4 weeks pain-free. Side plank with bottom knee (not ankle) resting on a bench or step — short lever reduces adductor moment arm. Hold hip elevated through the medial chain for the full duration. Short lever reduces adductor moment arm — the appropriate entry point. Targets adductor magnus and longus under the highest hip adductor demand in the program. Do not run concurrently with Adductor Isometric — this replaces it. Progression: advance to long lever (ankle on bench) when 3 × 30s is manageable.'}]},
```
Replace with:
```
               {n:'Copenhagen Plank — Short Lever',rx:'3 × 20–30s each side',d:'Phase 2+ adductor progression — replaces Adductor Side-Lying ISO after 2–4 weeks pain-free. Side plank with bottom knee (not ankle) resting on a bench or step — short lever reduces adductor moment arm. Hold hip elevated through the medial chain for the full duration. Short lever reduces adductor moment arm — the appropriate entry point. Targets adductor magnus and longus under the highest hip adductor demand in the program. Do not run concurrently with Adductor Isometric — this replaces it. Progression: advance to long lever (ankle on bench) when 3 × 30s is manageable.'},
               {n:'Single-Leg Calf Raise — Straight Knee',rx:'1 × 15 each · 3s down · Ph1',d:'Gastrocnemius load. Reduced to 1 set Phase 1 — soleus is higher priority. Full volume returns Phase 2+. Moved to Optional in v10.2 — prioritise frontal plane exercises in the main block.'},
               {n:'Single-Leg Calf Raise — Bent Knee (Soleus)',rx:'2 × 12–15 each · 3s down',d:'Soleus isolation — primary plantarflexor under all knee-flexed positions: wall sit, ski descent, split squat. Higher priority of the two calf raises. Moved to Optional in v10.2 — prioritise frontal plane exercises in the main block.'}]},
```

- [ ] **Step 3: Remove Calf Raise cards from src-knee HTML**

Find (in src-knee, the two calf raise cards):
```
      <div class="ex-card"><div class="ex-top"><span class="ex-name">Single-Leg Calf Raise — Straight Knee</span><span class="ex-rx">1 × 15 each · 3s down · Ph1</span></div><div class="ex-desc">Stand on one foot, full plantarflexion top, full dorsiflexion bottom. 3s eccentric. Gastrocnemius. Reduced to 1 set Phase 1 — soleus is higher priority. Full volume (2×15) returns Phase 2+. <strong>Levers:</strong> flat→step edge / 3s→5s eccentric / bodyweight→weighted.</div></div>
      <div class="ex-card"><div class="ex-top"><span class="ex-name">Single-Leg Calf Raise — Bent Knee (Soleus)</span><span class="ex-rx">2 × 12–15 each · 3s down</span></div><div class="ex-desc">Knee bent ~30°. Isolates soleus — primary plantarflexor under knee-flexed load: wall sit, step down, split squat, ski descent, cycling. Higher priority of the two calf raises. <strong>Levers:</strong> flat→step edge / 3s→5s eccentric / bodyweight→weighted / two legs→single leg.</div></div>
```
Replace with: *(delete both — replace with empty string)*

- [ ] **Step 4: Add Calf Raise cards to src-opt HTML**

Find (the last card in src-opt, Copenhagen Plank):
```
      <div class="ex-card"><div class="ex-top"><span class="ex-name">Copenhagen Plank — Short Lever</span><span class="ex-rx">3 × 20–30s each side</span></div><div class="ex-desc"><strong>Phase 2+ progression — replaces Adductor Side-Lying ISO after 2–4 weeks pain-free.</strong> Position: side plank with bottom <em>knee</em> (not ankle) resting on a bench or step. Top leg straight. Hold hips elevated through the medial chain for the full duration. Short lever reduces adductor moment arm — the appropriate entry point. Targets adductor magnus and longus under the highest hip adductor demand in the program. Do not run concurrently with Adductor Isometric — this replaces it. <strong>Progression:</strong> advance to long lever (ankle on bench) when 3 × 30s is manageable each side.</div></div>
    </div>
  </div>
```
Replace with:
```
      <div class="ex-card"><div class="ex-top"><span class="ex-name">Copenhagen Plank — Short Lever</span><span class="ex-rx">3 × 20–30s each side</span></div><div class="ex-desc"><strong>Phase 2+ progression — replaces Adductor Side-Lying ISO after 2–4 weeks pain-free.</strong> Position: side plank with bottom <em>knee</em> (not ankle) resting on a bench or step. Top leg straight. Hold hips elevated through the medial chain for the full duration. Short lever reduces adductor moment arm — the appropriate entry point. Targets adductor magnus and longus under the highest hip adductor demand in the program. Do not run concurrently with Adductor Isometric — this replaces it. <strong>Progression:</strong> advance to long lever (ankle on bench) when 3 × 30s is manageable each side.</div></div>
      <div class="ex-card"><div class="ex-top"><span class="ex-name">Single-Leg Calf Raise — Straight Knee</span><span class="ex-rx">1 × 15 each · 3s down · Ph1</span></div><div class="ex-desc">Stand on one foot, full plantarflexion top, full dorsiflexion bottom. 3s eccentric. Gastrocnemius. Reduced to 1 set Phase 1 — soleus is higher priority. Full volume (2×15) returns Phase 2+. Moved to Optional in v10.2 — prioritise frontal plane exercises in main block. <strong>Levers:</strong> flat→step edge / 3s→5s eccentric / bodyweight→weighted.</div></div>
      <div class="ex-card"><div class="ex-top"><span class="ex-name">Single-Leg Calf Raise — Bent Knee (Soleus)</span><span class="ex-rx">2 × 12–15 each · 3s down</span></div><div class="ex-desc">Knee bent ~30°. Isolates soleus — primary plantarflexor under knee-flexed load: wall sit, step down, split squat, ski descent, cycling. Higher priority of the two calf raises. Moved to Optional in v10.2 — prioritise frontal plane exercises in main block. <strong>Levers:</strong> flat→step edge / 3s→5s eccentric / bodyweight→weighted / two legs→single leg.</div></div>
    </div>
  </div>
```

- [ ] **Step 5: Verify**

Run: `grep -c "Calf Raise" index.html`
Note the total count. Calf Raise cards should be in src-opt only (not src-knee). Calf Raise entries should be in sA_opt only (not sA_knee). Run:
```bash
grep -n "Calf Raise" index.html
```
Confirm no line numbers fall in the ~2046–2056 range (src-knee) or ~2739–2748 range (sA_knee).

- [ ] **Step 6: Commit**
```bash
git add index.html
git commit -m "v10.2 — move Calf Raises to Optional pool, update knee block timing to ~20 min"
```

---

## Task 11: Create versioned output files + final commit

**Files:**
- Create: `kneehab V10.2.MD`
- Create: `kneehab V10.2.html`

- [ ] **Step 1: Final browser verification**

Open `index.html` in a browser. Check:
- Cover shows `v10.2`
- Load Day Today tab shows Asymmetry Rule in briefing
- Glute block: Adductor ISO → Hip Abduction → Single-Leg Glute Bridge (no Supine Hip ER)
- Hip Abduction shows `3 × 12 weak / 2 × 12 strong`
- Spinal & Shoulder: Bird Dog + WLYT only (no Face Pull)
- Core: Hollow Body Hold + Side Plank only (no RKC Plank)
- Knee block order: Wall Sit · Step Down · Short Arc Quad · Split Squat · Curtsy Squat · Cossack Squat · Hamstring Slide · Standing Hamstring ISO
- No Skater Squat in knee block
- Optional pool contains Calf Raises
- Reference hub Knee tab: Curtsy + Cossack cards present, Skater Squat absent, Calf Raises absent
- Reference hub Optional tab: Calf Raise cards present

- [ ] **Step 2: Create `kneehab V10.2.MD`**

Create the file with this content:
```markdown
# KneeHab V10.2 — Asymmetry Rule + Frontal Plane Rebuild

**Date:** 2026-03-30
**Change type:** Medium (12 changes across 4 blocks)

## Summary

Introduces the Asymmetry Rule protocol and rebuilds the Load day around the confirmed right-side glute medius deficit. Load day trimmed by removing Recovery-day duplicates (Supine Hip ER, RKC Plank, Face Pull) and reducing Couch Stretch. Skater Squat removed as too advanced for current frontal plane capacity. Curtsy Squat and Cossack Squat added as direct glute med training in adducted/lateral loading patterns. Calf Raises moved to Optional pool.

## Changes

### Asymmetry Rule (new protocol)
Added to Load Day briefing: weaker side first on all alternating exercises, extra set to weaker side.

### Mobility Block
- **Couch Stretch:** 2 × 30s → 1 × 30s on Load day. Full volume retained on Recovery day.

### Spinal & Shoulder Block (Load Day)
- **Face Pull:** removed from Load day. Recovery day unchanged.

### Core Block (Load Day)
- **RKC Plank:** removed from Load day. Recovery day unchanged. Side Plank is more directly relevant to frontal plane work that follows.

### Glute Block (Load Day)
- **Supine Hip ER with Block:** removed from Load day. Recovery day unchanged.
- **Hip Abduction:** rx updated to `3 × 12 weak / 2 × 12 strong · 2s hold top`. Asymmetry rule applied.
- **Adductor ISO Hold:** asymmetry rule applied to description.

### Knee & Lower Body Block
- **Slant Board Step Down:** pre-activation cue added — 5s single-leg stance, level pelvis, before weaker-side sets.
- **Skater Squat:** removed. Frontal plane control not yet sufficient for single-leg demand.
- **Curtsy Squat:** added. `3 × 8 weak / 2 × 8 strong · 2s descent`. Bilateral crossed stance, high glute med demand in adducted position. Asymmetry rule applies.
- **Cossack Squat:** added. `3 × 5 weak / 2 × 5 strong · 3s descent · 3s ascent`. Lateral movement pattern with full regression ladder (Assisted → Elevated Heel → Partial Depth → Full → Weighted). Start at R1. Asymmetry rule applies.
- **Calf Raise ×2:** moved to Optional pool.
- Block timing updated: ~17 min → ~20 min.

### Optional Pool
- **Calf Raise ×2:** added. Joins Copenhagen Plank, Hamstring ISO in Flexion, TKE, Single-Leg Wall Sit, Hanging Knee Raise.

## Rationale

Frontal plane deficiency (right glute med weakness) is the primary upstream driver of the patient's current compensation patterns. Exercises are reorganised to directly address this deficit at every level of demand: isolated (Hip Abduction), bilateral-crossed (Curtsy Squat), and lateral-pattern (Cossack Squat). The Asymmetry Rule ensures the weaker side receives more stimulus each session without adding global volume. Load day trimmed to offset the added knee block time — total session time is approximately neutral.

## Foundations
Baar · Bellemans · ATG · Asymmetry Protocol
```

- [ ] **Step 3: Copy index.html to versioned snapshot**

```bash
cp "index.html" "kneehab V10.2.html"
```

- [ ] **Step 4: Final commit**

```bash
git add index.html "kneehab V10.2.html" "kneehab V10.2.MD"
git commit -m "v10.2 — Asymmetry Rule + frontal plane rebuild (Curtsy + Cossack, trim Load day)"
```
